## 1. 流式增量场景测试

- [x] 1.1 添加"流式增量场景" describe 块，包含：内容逐步追加时 split point 推进、内容缩短时回退、追加到未关闭代码块中
- [x] 1.2 验证流式增量场景测试全部通过

## 2. LLM 输出模式测试

- [x] 2.1 添加"LLM 输出模式" describe 块，包含：纯代码块消息、极短消息（单字符/单换行）、连续调用一致性、以换行符开头的内容
- [x] 2.2 验证 LLM 输出模式测试全部通过

## 3. 复杂 Markdown 结构测试

- [x] 3.1 添加"复杂 Markdown 结构" describe 块，包含：列表项间空行、引用块内空行、标题前后空行、文本中 ``` 但非代码围栏、HTML 标签内空行
- [x] 3.2 验证复杂 Markdown 结构测试全部通过

## 4. 极端边界测试

- [x] 4.1 添加"极端边界" describe 块，包含：仅围栏标记、连续多个空行、超长单行内容、CRLF 换行符
- [x] 4.2 验证极端边界测试全部通过

## 5. 全量验证

- [x] 5.1 运行全部测试，确认原有测试不受影响、新增测试全部通过
