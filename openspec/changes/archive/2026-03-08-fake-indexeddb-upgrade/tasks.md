## 1. 准备工作

- [x] 1.1 运行完整测试套件建立基线（`pnpm test:all`）
- [x] 1.2 记录基线测试结果（通过的测试数量、失败列表）
- [x] 1.3 保存测试输出日志以便对比

## 2. 升级依赖

- [x] 2.1 更新 `package.json` 中的 `fake-indexeddb` 版本从 `5.0.2` 到 `6.2.5`
- [x] 2.2 运行 `pnpm install` 更新 `pnpm-lock.yaml`
- [x] 2.3 验证 `package.json` 和 `pnpm-lock.yaml` 中的版本正确

## 3. 升级后验证

- [x] 3.1 运行完整测试套件（`pnpm test:all`）
- [x] 3.2 对比测试结果与基线，识别失败测试
- [x] 3.3 如果测试通过率 > 80%，进入修复阶段；否则评估回滚

**结果**: 测试通过率 100%，跳过阶段 4（修复）

## 4. 修复测试问题（如有需要）

### 4.1 错误类型变更适配
- [ ] 4.1.1 搜索测试文件中使用 `try/catch` 和 `.toThrow()` 的代码
- [ ] 4.1.2 识别依赖 `error.message` 或 `error.constructor` 的断言
- [ ] 4.1.3 更新断言以兼容 `DOMException` 类型（如需要）
- [ ] 4.1.4 运行相关测试验证修复

### 4.2 autoIncrement 行为变更适配
- [ ] 4.2.1 搜索使用 `autoIncrement: true` 的测试代码
- [ ] 4.2.2 检查 keyPath 值可能为 `undefined` 的场景
- [ ] 4.2.3 调整测试数据或断言以符合新行为（如需要）
- [ ] 4.2.4 运行相关测试验证修复

### 4.3 事件调度时序适配
- [ ] 4.3.1 识别使用 `fake-timers` 或时间相关的测试
- [ ] 4.3.2 检查是否有异步时序问题
- [ ] 4.3.3 添加显式等待或调整时序断言（如需要）
- [ ] 4.3.4 运行相关测试验证修复

### 4.4 其他问题修复
- [ ] 4.4.1 分析其他失败测试的根本原因
- [ ] 4.4.2 修复测试代码（非生产代码）
- [ ] 4.4.3 运行修复的测试验证

## 5. 最终验证

- [x] 5.1 运行完整测试套件（`pnpm test:all`）
- [x] 5.2 运行代码检查（`pnpm lint`）
- [x] 5.3 运行类型检查（`pnpm tsc`）
- [x] 5.4 确认所有测试通过且无 lint/类型错误

## 6. 文档更新

- [x] 6.1 在 `AGENTS.md` 中添加 `fake-indexeddb` 相关说明
- [x] 6.2 记录当前版本要求（6.2.5）
- [x] 6.3 记录已知的潜在问题和解决方案
- [x] 6.4 添加升级决策和经验总结

## 7. 回滚准备（如需要）

- [ ] 7.1 评估回滚必要性（测试失败率 > 20% 或耗时 > 1 小时）
- [ ] 7.2 如需回滚，执行 `git checkout package.json pnpm-lock.yaml`
- [ ] 7.3 运行 `pnpm install` 恢复依赖
- [ ] 7.4 运行 `pnpm test:all` 验证回滚成功
- [ ] 7.5 记录回滚原因和后续行动计划
