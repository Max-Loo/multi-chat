## 1. 布局调整

- [x] 1.1 将密钥导出卡片改为左右布局：卡片整体 flex-row items-center justify-between，左侧标题+描述（flex-col），右侧按钮竖直居中
- [x] 1.2 将重置数据卡片改为同样的左右布局结构
- [x] 1.3 确保按钮使用 `shrink-0` 自适应宽度，左侧内容使用 `flex-1 min-w-0`

## 2. 验证

- [x] 2.1 确认页面渲染正确，按钮在整个卡片容器内竖直居中
