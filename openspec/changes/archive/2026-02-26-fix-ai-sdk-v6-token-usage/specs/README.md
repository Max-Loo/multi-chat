# 规范变更说明

## 概述

本变更（`fix-ai-sdk-v6-token-usage`）是纯实现层面的 bug 修复，不涉及规范级别的行为变更。

## 原因

根据 `proposal.md` 的 Capabilities 部分：
- **New Capabilities**: 空（本变更不引入新功能）
- **Modified Capabilities**: 空（本变更仅修复实现细节，不改变需求级别的行为）

## 影响

- **外部 API**: 无变化
- **系统行为**: 无变化
- **类型定义**: 仅内部实现使用正确的类型，外部接口保持稳定

## 结论

由于没有规范级别的变更，本 `specs/` 目录不包含任何具体的规范文件。
