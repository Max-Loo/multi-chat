## 1. 代码修改

- [x] 1.1 修改滚动到底部按钮的主样式类，将蓝色主题改为深灰色主题（`bg-gray-900 text-white`）
- [x] 1.2 调整按钮尺寸从 `h-12 w-12` 改为 `h-10 w-10`，与发送按钮保持一致
- [x] 1.3 调整按钮位置从 `bottom-24` 改为 `bottom-[110px]`，避免被发送框遮挡
- [x] 1.4 添加阴影效果和 hover 状态（`shadow-md hover:shadow-lg hover:bg-gray-800`）
- [x] 1.5 修改加载状态样式，将蓝色边框改为灰色边框（`border-gray-300 border-t-gray-600`）
- [x] 1.6 移除蓝色相关的样式类（`border-blue-300`、`border-t-blue-500`、`text-blue-500` 等）

## 2. 测试验证

- [ ] 2.1 启动开发服务器，验证按钮在聊天面板中的显示效果
- [ ] 2.2 测试按钮仅在需要滚动条且不在底部时正确显示
- [ ] 2.3 验证点击按钮能正确滚动到消息列表底部
- [ ] 2.4 测试发送中状态下的加载动画效果（灰色旋转边框）
- [ ] 2.5 在不同窗口大小下测试按钮是否被发送框遮挡
- [ ] 2.6 验证 hover 状态下的视觉过渡效果

## 3. 代码检查

- [x] 3.1 运行 `pnpm lint` 确保代码符合 ESLint 规范
- [x] 3.2 运行 `pnpm tsc` 确保 TypeScript 类型检查通过
- [x] 3.3 检查修改后的代码是否符合项目代码风格（使用 `@/` 导入路径等）

## 4. 可选优化

- [x] 4.1 考虑添加过渡动画使按钮位置变化更平滑（如果需要）
- [ ] 4.2 验证在暗色模式下的显示效果（如果项目支持）
