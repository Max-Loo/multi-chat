## 1. 准备

- [x] 1.1 检查并安装 shadcn/ui Separator 组件（若未安装则使用 `border-t` 降级方案）

## 2. 调整错误 Alert 间距

- [x] 2.1 为每个 `<Alert>` 增加内边距至 `p-4`，增大标题与描述的行间距
- [x] 2.2 增大错误列表容器中各 Alert 之间的间距至 `gap-3`

## 3. 重构按钮区布局

- [x] 3.1 将刷新按钮设为独占一行，保持 `size="lg"` 和默认样式
- [x] 3.2 在刷新按钮下方添加分割线（Separator 或 `border-t`）
- [x] 3.3 将主密钥恢复按钮和重置数据按钮改为横向并排（`flex-row`），间距 `gap-3`
- [x] 3.4 添加 `flex-wrap` 确保窄屏幕下按钮自动换行

## 4. 验证

- [x] 4.1 在 dark 和 light 主题下确认视觉效果
- [x] 4.2 确认仅刷新+重置（无主密钥错误）场景的布局正确
- [x] 4.3 确认三个按钮同时显示时的布局正确
