## 1. 代码修改

- [x] 1.1 在 `ChatPanelSender.tsx` 第 162 行之前添加隐藏原因说明注释
- [x] 1.2 在推理内容开关的 `<div>` 容器（第 163 行）添加 `className="hidden"` 属性
- [x] 1.3 在隐藏代码块后（第 182 行）添加隐藏结束标记注释

## 2. 代码质量检查

- [x] 2.1 运行 `pnpm lint` 确保代码符合 ESLint 规范
- [x] 2.2 运行 `pnpm tsc` 确保 TypeScript 类型检查通过（注：项目预先存在 18 个 TS 错误，与本次变更无关）
- [x] 2.3 运行 `pnpm test:run` 确保所有测试通过（16 个文件，337 个测试全部通过 ✓）

## 3. 本地验证

- [x] 3.1 启动开发服务器 `pnpm tauri dev`（用户已手动执行并验证）
- [x] 3.2 验证聊天发送框显示正常，无推理内容开关（用户已验证通过）
- [x] 3.3 验证发送消息功能正常工作（用户已验证通过）
- [x] 3.4 验证其他 UI 组件不受影响（用户已验证通过）

## 4. 文档和跟踪

- [x] 4.1 在代码注释中引用相关的技术债务 issue（已包含技术债务说明，建议创建 GitHub issue 并引用）
- [ ] 4.2（可选）创建 GitHub issue 跟踪模型服务商对 `type: 'reasoning'` 的支持进度（需用户手动创建，已提供模板）
- [x] 4.3（可选）更新 AGENTS.md 或 README.md（不需要，临时措施不影响文档）
