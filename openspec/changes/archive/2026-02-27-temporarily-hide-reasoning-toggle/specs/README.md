# Specs 目录

此变更不需要创建任何规范（spec）文件。

## 原因

根据 `proposal.md` 中的 **Capabilities** 部分：
- **New Capabilities**: 无新功能引入
- **Modified Capabilities**: 无功能需求变更。仅 UI 层面的隐藏，不涉及功能规格调整

此变更仅涉及 UI 隐藏（通过 CSS 类），不改变任何功能需求或系统行为，因此不需要创建规范文件。
